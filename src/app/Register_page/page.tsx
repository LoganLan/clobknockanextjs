// Correct structure for a React component
import React from 'react';

const RegisterPage = () => {
  return (
    <div>
      <h1>Register</h1>
      
    </div>
  );
};

export default RegisterPage; // Make sure this is the default export
